[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dream-region-pilot"
version = "0.1.0"
description = "Minimal training-free regional asynchronous decoding pilot for Dream"
requires-python = ">=3.10,<3.13"
dependencies = [
  "accelerate==1.4.0",
  "datasets==3.3.2",
  "huggingface-hub==0.34.5",
  "matplotlib==3.10.6",
  "numpy==2.1.2",
  "protobuf==6.31.1",
  "pyyaml==6.0.2",
  "safetensors==0.5.3",
  "sentencepiece==0.2.1",
  "tokenizers==0.20.3",
  "torch==2.5.1",
  "tqdm==4.67.1",
  "transformers==4.46.2",
]

[project.optional-dependencies]
evaluation = [
  "antlr4-python3-runtime==4.11.0",
  "math-verify==0.9.0",
  # Keep this aligned with torch 2.5.1; Math-Verify only requires unpinned
  # SymPy through latex2sympy2_extended.
  "sympy==1.13.1",
]

[project.scripts]
dream-region-pilot = "dream_region_pilot.run_gsm8k:main"

[tool.setuptools.packages.find]
where = ["src"]
